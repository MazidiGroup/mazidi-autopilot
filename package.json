{
  "name": "mazidi-autopilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic orchestration control plane for Mazidi Group. AI handles judgment; this software handles control.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "migrate": "node dist/src/migrate.js",
    "heartbeat": "node dist/src/heartbeat-cli.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
